import { formatCurrency } from '../../shared/lib/formatters';
import { CartIcon, CloseIcon } from './Icon';

export function CartDrawer({ open, cart, total, onClose, onChangeQuantity, onRemove, onCheckout }) {
  return <><button aria-label="Fechar carrinho" onClick={onClose} className={`fixed inset-0 z-40 bg-black/50 transition-opacity ${open ? 'opacity-100' : 'pointer-events-none opacity-0'}`} /><aside className={`fixed right-0 top-0 z-50 flex h-full w-full max-w-sm translate-x-full flex-col bg-white shadow-2xl transition-transform dark:bg-brand-900 ${open ? 'translate-x-0' : ''}`} aria-label="Carrinho de compras"><div className="flex items-center justify-between border-b border-brand-100 px-4 py-4 dark:border-brand-700"><h2 className="text-lg font-bold dark:text-white">Meu Carrinho</h2><button onClick={onClose} aria-label="Fechar carrinho" className="icon-button text-brand-900 dark:text-white"><CloseIcon /></button></div>{cart.length ? <div className="flex-1 divide-y divide-brand-100 overflow-y-auto px-4 dark:divide-brand-700">{cart.map((item) => <div key={item.productId} className="flex gap-3 py-3"><img src={item.imageUrl} alt="" className="h-14 w-14 rounded-lg bg-brand-100 object-cover" /><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold dark:text-white">{item.name}</p><p className="mt-0.5 text-xs text-brand-500 dark:text-brand-300">{formatCurrency(item.price)} un.</p><div className="mt-2 flex items-center gap-2"><button onClick={() => onChangeQuantity(item.productId, -1)} className="quantity-button">−</button><span className="w-5 text-center text-sm">{item.quantity}</span><button onClick={() => onChangeQuantity(item.productId, 1)} className="quantity-button">+</button></div></div><button onClick={() => onRemove(item.productId)} className="self-start p-1 text-brand-400 hover:text-red-500" aria-label={`Remover ${item.name}`}>×</button></div>)}</div> : <div className="flex flex-1 flex-col items-center justify-center px-6 text-center text-brand-400"><CartIcon className="mb-3 h-14 w-14 opacity-40" /><p className="font-medium">Seu carrinho está vazio</p><p className="mt-1 text-sm">Adicione produtos para começar seu pedido.</p></div>}<div className="border-t border-brand-100 px-4 py-4 dark:border-brand-700"><div className="mb-3 flex justify-between"><span className="font-medium text-brand-500 dark:text-brand-300">Subtotal</span><span className="text-xl font-extrabold dark:text-white">{formatCurrency(total)}</span></div><button disabled={!cart.length} onClick={onCheckout} className="w-full rounded-xl bg-accent-500 py-3 font-bold text-white disabled:pointer-events-none disabled:opacity-40 hover:bg-accent-600">Finalizar no WhatsApp</button></div></aside></>;
}
